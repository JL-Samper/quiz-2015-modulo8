var express = require('express');
var router = express.Router();

/* GET home page. */
router.get('/', quizController.index);

router.get('/author',quizController.show);

router.get('/quizes/:quizId',quizController.answer);

module.exports = router;
