<table>
 <!-- recibimos todos los valores desde el controlador al hacer
 models.Quiz.findAll().then( function (quizes) ... -->
<% var i; for (i=0; i< quizes.length; i++) { %> 
    <tr><td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></td></tr>
<% } %>
</table>